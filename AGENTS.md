# AGENTS.md

## Status
**COLD / INACTIVE PLACEHOLDER / EXPLICIT REFERENCE ONLY**

This repository is not the active RTS core, not RTS-AGE, and not the preferred lightweight runtime reference.

## Load rule
Do not include this repository in ordinary cross-repository discovery or implementation planning. Open it only when the task explicitly concerns the historical `rts-lite` placeholder or whether it should remain archived/inactive.

If a lightweight RTS reference is needed, use `RTS-Minimal-Runtime` instead unless a newer canonical decision says otherwise.

## Hard boundaries
- No new implementation by default.
- Do not duplicate active RTS responsibilities here.
- Do not treat placeholder code/files as current architecture evidence.

## Stop condition
If the task is not specifically about this placeholder, stop reading this repository and route to the active canonical source.